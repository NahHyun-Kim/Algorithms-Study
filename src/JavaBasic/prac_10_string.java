package JavaBasic;

public class prac_10_string {

	public static void main(String[] args) {
		/*
		 * 문자열을 표현하기 위해 사용, char데이터 타입이 아닌 String 객체 사용을 권장
		 * */
		
		String s;

		//위의 함수 맨 끝의 s는 cases가 아니라 IgnoreCase(String s)를 붙인 것이다.
		int compareToS; //문자열을 사전 순으로 비교해 정수 값을 반환
		int compareToIgnoreCaseS; //대소문자 무시, 비교
		boolean equalsS; //주어진 s와 현재 문자열을 비교후, true/false로 반환한다.
		boolean equlalsIgnoreCaseS; //대소문자 없이 비교
		char charAt; //(int index); index가 지정한 문자를 반환한다.
		String concatS; //주어진 문자열 s를 현재 문자열 뒤에 붙인다.
		boolean containsS; //문자열 s를 포함하는지 조사한다.
		boolean endsWithS; //끝나는 문자열이 s인지 조사한다.
		boolean isEmpty; //문자열의 길이가 0이면 true를 반환한다.
		int length; //문자열이 길이 반환
		boolean startsWithS; //시작하는 문자열이 s인지 조사한다.
		String substring; //(int index); index부터 시작하는 문자열의 일부 반환한다.
		String toLowerCase; //문자열을 모두 소문자로 변환한다.
		String toUpperCase; //문자열을 모두 대문자로 변환한다.
		String trim; //문자열 앞뒤에 있는 공백을 제거한 후 변환한다.
		
	}

}
