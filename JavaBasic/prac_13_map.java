package JavaBasic;

public class prac_13_map {

	public static void main(String[] args) {
		/*
		 * Map 인터페이스
		 * 키와 값을 쌍으로 저장하며, 키는 중복되지 않는다. HashMap을 가장 많이 사용
		 * 
		 * void clear(); 모든 매핑 삭제
		 * boolean containsKey(Object key); 주어진 키의 존재 여부 반환
		 * boolean containsValue(Object value); 주어진 값의 존재 여부 반환
		 * Set<Map, Entry<K,V>> entrySet(); 모든 매핑을 Set타입으로 반환
		 * V get(Object key); 주어진 키에 해당하는 값 반환
		 * boolean isEmpty(); 컬렉션이 비어 있는지 여부 반환
		 * Set<K> keySet(); //모든 키를 Set 타입으로 반환한다.
		 * V put (K key, V value) 주어진 키-값을 저장하고 값을 반환한다
		 * V remove(Object key); 키와 일치하는 원소(V)를 삭제하고 값을 반환한다.0
		 * int size(); 컬렉션의 크기를 반환한다.
		 * Collection<V> values() 모든 값을 Collection 타입으로 반환한다.
		 * 
		 */
	}

}
